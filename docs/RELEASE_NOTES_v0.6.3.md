# rest-sample-dubbo-consumer 0.6.3

`0.6.3` aligns the runnable consumer with `rust-java-rest:4.4.1`. Dubbo `0.7.2`, REST ABI `28`, and
Dubbo ABI `7` remain unchanged.

Static/Kubernetes Service discovery, ZooKeeper discovery, generated clients, routes, and Java
business code are unchanged.
